export enum Sizes {
  XS = "xs",
  SM = "sm",
  MD = "md",
  LG = "lg",
  XL = "xl",
}

export type TSizes = "xs" | "sm" | "md" | "lg" | "xl";
export type TColors = "primary" | "warning" | "success" | "error" | "gray";
